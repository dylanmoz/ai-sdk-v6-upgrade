# Vercel AI SDK v6 Issue Reproduction

Minimal repro environment for Vercel AI SDK issue

## Setup

```bash
npm install --registry https://registry.npmjs.org/
```

## Build

```bash
npx tsc
```

Check for TypeScript errors in the output.

Set ai package to `"ai": "5.0.125"` to see the build succeed without errors.
